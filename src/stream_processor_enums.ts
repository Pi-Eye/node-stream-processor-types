// Enum classes
export enum ArgType { kRequired = 0, kText = 1, kMotion = 2, kDevice = 3 }
export enum Format { kRGB = 0, kGray = 1 }
export enum TextPosition { kTop = 0, kBottom = 1 }
export enum DeviceType { kCPU = 0, kGPU = 1, kSpecific = 2 }